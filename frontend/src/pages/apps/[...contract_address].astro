---
import apps from '../../content/apps.json';

export async function getStaticPaths() {
	return apps.map((app) => ({
		params: { contract_address: app.contract_address },
		props: app,
	}));
}


const app = Astro.props;
---

{app.contract_address}
{app.deployer_address}
{app.daily_active_addresses}
{app.weekly_active_addresses}
{app.monthly_active_addresses}
{app.tvl}
{app.avg_volume}
{app.liquidity}
{app.admin_fns}
{app.upgradeability}
