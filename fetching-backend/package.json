{
  "name": "fetching-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build": "cargo build --release",
    "start": "./target/release/defi-explorer",
    "start:wscat": "mkdir -p db/filtered_bytecodes; mkdir -p db/fetched_addresses; wscat -c ws://127.0.0.1:9002",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wscat": "^5.2.0"
  }
}
